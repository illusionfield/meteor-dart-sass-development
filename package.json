{
  "name": "illusionfield-meteor-dart-sass-development",
  "scripts": {
    "test:once": "mtest --package ./ --port=8888 --once",
    "test:watch": "mtest --package ./ --port=8888",
    "test:browser": "meteor test-packages ./ --port 8888"
  },
  "devDependencies": {
    "@zodern/mtest": "^0.5.1",
    "sass": "1.79.6"
  }
}
